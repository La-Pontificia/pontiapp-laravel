<h1>META</h1>

<p>
    @if ($id_colab)
        Colaborador
        {{ $id_colab }}
    @endif

    @if ($id_eda)
        Eda
        {{ $id_eda }}
    @endif

    @if ($id_eva)
        Evaluacion
        {{ $id_eva }}
    @endif
</p>
